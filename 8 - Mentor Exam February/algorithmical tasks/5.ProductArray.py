# Given an integer array nums, return an array where output[i] 
# is the product of all elements except nums[i], without using division.
# Input: A list nums (length ≥ 2).
# Output: A list where each element is the product of the rest.
